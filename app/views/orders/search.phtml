<?php
/**
 * @var \Phalcon\Mvc\View\Engine\Php $this
 */
?>

<?php use Phalcon\Tag; ?>

<div class="row">
    <nav>
        <ul class="pager">
            <li class="previous"><?php echo $this->tag->linkTo(["orders/index", "Go Back"]); ?></li>
            <li class="next"><?php echo $this->tag->linkTo(["orders/new", "Create "]); ?></li>
        </ul>
    </nav>
</div>

<div class="page-header">
    <h4>Search result</h4>
</div>

<?php echo $this->getContent(); ?>

<div class="row">
    <table class="table table-bordered">
        <thead>
            <tr>
                <th>Order</th>
            <th>User</th>
            <th>Products</th>

                <th></th>
                <th></th>
            </tr>
        </thead>
        <tbody>
        <?php foreach ($page->items as $order): ?>
            <tr>
                <td><?php echo $order->order_id ?></td>
            <td><?php echo $order->user_id ?></td>
            <td><?php echo $order->products ?></td>

                <td><?php echo $this->tag->linkTo(["orders/edit/" . $order->getOrderId(), "Edit"]); ?></td>
                <td><?php echo $this->tag->linkTo(["orders/delete/" . $order->getOrderId(), "Delete"]); ?></td>
            </tr>
        <?php endforeach; ?>
        </tbody>
    </table>
</div>

<div class="row">
    <div class="col-sm-1">
        <p class="pagination" style="line-height: 1.42857;padding: 6px 12px;">
            <?php echo $page->current, "/", $page->total_pages ?>
        </p>
    </div>
    <div class="col-sm-11">
        <nav>
            <ul class="pagination">
                <li><?php echo $this->tag->linkTo(["orders/search", "First", 'class' => 'page-link']) ?></li>
                <li><?php echo $this->tag->linkTo(["orders/search?page=" . $page->before, "Previous", 'class' => 'page-link']) ?></li>
                <li><?php echo $this->tag->linkTo(["orders/search?page=" . $page->next, "Next", 'class' => 'page-link']) ?></li>
                <li><?php echo $this->tag->linkTo(["orders/search?page=" . $page->last, "Last", 'class' => 'page-link']) ?></li>
            </ul>
        </nav>
    </div>
</div>
